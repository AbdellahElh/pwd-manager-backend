DATABASE_URL="file:./dev.db"
PORT=3000
JWT_SECRET="your_jwt_secret_key_here" # Generate a secure random string for production
NODE_ENV="development"  # Set to "production" in production environment
ENFORCE_HTTPS="false"   # Set to "true" in production environment
ENCRYPTION_SALT="random_hex_string_32_chars" # Generate with: openssl rand -hex 16
APP_SECRET_KEY="app-secret-key-for-encryption-DO-NOT-SHARE" # Must match frontend VITE_SECRET_KEY
